ADMIN_USERNAME=
ADMIN_PASSWORD=

# E164 format without + (e.g. 2348012345678). Used for WhatsApp button and footer phone.
NEXT_PUBLIC_WHATSAPP_NUMBER=

# Full Instagram profile URL (header on mobile + footer when site settings leave this blank).
NEXT_PUBLIC_INSTAGRAM_URL=

# Optional TikTok URL when Admin site settings TikTok field is empty (footer + mobile header still use a built-in default).
NEXT_PUBLIC_TIKTOK_URL=

SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=model-images

